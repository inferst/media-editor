import { TextOptions } from "@/types";

export const getDefaultTextOptions: () => TextOptions = () => ({
  font: "Roboto",
  alignment: "left",
  style: "noframe",
  color: "#ffffff",
  size: 24,
});
